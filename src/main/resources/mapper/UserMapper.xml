<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ipet.mapper.UserMapper">

  <resultMap id="BaseResultMap" type="com.ipet.model.User">
    <id column="id"  property="id" />
    <result column="username" property="username" />
    <result column="password" property="password" />
    <result column="nickName" property="nickName" />
    <result column="avatarUrl" property="avatarUrl" />
    <result column="roleId" property="roleId" />
  </resultMap>

  <select id="getUsers" resultMap="BaseResultMap">
    SELECT
    	id,username,password,nickName,avatarUrl,roleId
    FROM
    	t_user
  </select>
  
  <select id="getUserByUsername" resultMap="BaseResultMap">
    SELECT
    	id,username,password,nickName,avatarUrl,roleId
    FROM
    	t_user
    WHERE
    	username=#{username}
  </select>
  
  <insert id="addUser" parameterType="com.ipet.model.User">
  	INSERT INTO
  		t_user(username,password,nickName,avatarUrl,roleId)
  	VALUES
  		(#{username},#{password},#{nickName},#{avatarUrl},#{roleId})
  </insert>
  
  <update id="updateUser">
  	UPDATE
  		t_user
  	SET 
  		password=#{password},
  		nickName=#{nickName},
  		avatarUrl=#{avatarUrl},
  		roleId=#{roleId}
  	WHERE
  		id=#{id}
  </update>
  
  <delete id="delUser">
  	DELETE FROM
  		t_user
  	WHERE
  		id=#{id}
  </delete>

</mapper>